@using test0926_MVC.Models;
@model testTable

@{
    ViewBag.Title = "Create";
}

<h2>Edit</h2>

<form method="post" action="">
    <input type="hidden" name="id" id="id1" value="@Model.id" />

    <div class="form-group">
        <label>Id: @Model.id</label>
    </div>

    <div class="form-group">
        <label>charData:</label> <input type="text" name="charData" value="@Model.charData" class="form-control" /><br />
    </div>
    <label>IntDat:</label>

    <select name="intData" class="form-control">
        <option value="2" @( (Model.intData == 2) ? "selected" : "" )>TaiPei</option>
        <option value="4" @( (Model.intData == 4) ? "selected" : "" )>TaiChung</option>
        <option value="6" @( (Model.intData == 6) ? "selected" : "" )>TaiNan</option>
    </select>
    <br />

    <input type="submit" name="OkButton" value="&nbsp;&nbsp;OK&nbsp;&nbsp;" class="btn btn-lg btn-success" />
    <a href="~/Home/Index" class="btn btn-danger btn-lg">Home/Cancel</a>
</form>


